# Spring properties
spring:
  application:
     name: user-services

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

# HTTP Server
server:
  port: 8761   # HTTP (Tomcat) port